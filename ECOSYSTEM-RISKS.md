# 开源打赏 UGC 生态：风险清单与治理方案 (ecosystem-risks) v1.0

> 配套文档：本文件与 `ETHICS.md`（伦理规范）配合使用——ETHICS 定"应该怎样"，本文件定"可能烂在哪、怎么防"。
> 思路：**透明 = 信任；伦理 = 秩序；链上 = 防刷；注册表 = 可审计。**

## 一、诚信类

| # | 风险 | 场景 | 解决方案 / 规避 |
|---|------|------|----------------|
| R1 | 虚假贡献者 | 收钱不交付、跑路、套马甲收钱 | ① 未验证徽章（已有）；② 交付承诺字段（`bio`/`verifiedBy`）；③ 举报机制（v1.1）；④ 大额打赏前提醒"未验证" |
| R2 | 冒名骗打赏 / 归属误导（原"冒领/抄袭骗打赏"） | 复制他人开源插件、换马甲、挂自己地址，让受益人误以为在支持原作者 → 收割本属原作者的打赏 | ① 生态认证（GitHub 绑定，P3 规划）；② 归属声明 `plugins[].contributorId` 强制引用（校验已有）；③ **`upstream` 来源声明 = 署名权在打赏场景的延伸（§5.4）**；④ 冒名举报（分类"抄袭/冒领"→ 归入"归属误导"） |

> **术语校准（v1.1）**：本表"防抄袭"系列正式改名为「**冒名骗打赏 / 归属误导**」。开源许可证（MIT/Apache/GPL）把代码使用权让渡给所有人、只保留**署名义务**——抄代码是许可内的行为（开源本就鼓励），**冒名才是违约/欺诈**。治理机制防的是"受益人被误导"（归属问题），不是"代码被复制"（许可问题）。全文相关表述随此校准。
| R3 | 刷榜/刷热度 | 自打赏刷致谢墙、水军刷支持数 | ① **链上雷达天然防刷**：金额必须是真实链上转账（无法伪造）；② 榜单标注"链上确认"；③ 支持人数按链上收款地址**去重**统计 |
| R4 | 虚假承诺 | 订阅收了钱不提供额外交付 | ① ETHICS 红线（订阅须对应交付）；② 举报 + 标记；③ 定期"贡献者存续"提示 |

## 二、信任与安全类

| # | 风险 | 场景 | 解决方案 / 规避 |
|---|------|------|----------------|
| R5 | 收款码滥用 | 微信/支付宝收款码用于非法收款 | ① 免责声明（平台不担保收款用途）；② 自愿性声明；③ 举报下线通道 |
| R6 | 钓鱼链接 | 打赏链接指向恶意站点 | ① URL 校验（http(s)，已有）；② 外链新窗口打开 + "离开 DSH"提示；③ 举报；④ 域名黑名单（v1.1，社区维护） |
| R7 | 隐私泄露 | 打赏者身份被收集/追踪 | ① ETHICS 红线：不收集打赏者身份（已有）；② 伪匿名默认；③ 链上打赏本身就是伪名的 |
| R8 | 地址误导 | 展示地址与实际收款不符 | ① 雷达对账（打赏者可用链上浏览器核验）；② 地址不可更改提示（变更需重新声明 + 历史标记） |

## 三、经济与激励类

| # | 风险 | 场景 | 解决方案 / 规避 |
|---|------|------|----------------|
| R9 | 头部化/马太效应 | 头部拿走 90% 打赏，长尾饿死 | ① 公开承认是市场常态，不搞强制再分配；② 榜单按"人数"与"金额"双维，给小项目被看到的机会；③ 引导"小而美"生态位（明确价值主张比盲目做大更有效） |
| R10 | 投机套利/洗钱嫌疑 | 批量开插件号刷打赏、借打赏洗钱 | ① 链上可追溯（USDC 全公开）；② 面板提示"大额打赏请核实贡献者身份"；③ 平台配合执法（如有） |
| R11 | 加密价格波动 | 打赏价值随币价波动 | ① 面板标注"价值以到账时为准"；② 雷达金额展示按到账块价格口径统一（不实时换算法币，避免误导） |
| R12 | 支付渠道抽成不透明 | 法币平台抽成使打赏缩水 | ① 面板标注平台抽成提示（"平台可能抽成 X%"）；② 零抽成的加密直连置顶推荐 |

## 四、伦理与治理类

| # | 风险 | 场景 | 解决方案 / 规避 |
|---|------|------|----------------|
| R13 | 强制打赏/付费墙 | 功能锁在打赏后（人类） | ETHICS 红线 + 🟢⚪🔴 徽章（已实现） |
| R14 | **智能体强制付费** | Agent 被"必须先打赏才能用工具"绑架 | **v1.1 专项**：智能体打赏也须①自愿（Agent 可拒绝）②可审计（链上/日志）③可退出（无需授权持续扣费）；manifest 加 `agentPolicy` 字段声明 |
| R15 | 打赏绑架性索要 | 以"不打赏就不维护/给差评"施压 | ETHICS 红线：禁止"以打赏为条件的负面行为" |
| R16 | 生态割据 | 多个打赏标准互不兼容 | ① manifest `sponsor` 字段统一提案（你已在做）；② 标准以 CC0 开放，鼓励采纳 |
| R17 | 治理真空 | 出现问题无人裁决 | ① 分级处理：投诉→社区评审→标记/下线；② v1.1 定义"违规标记"机制（R1/R4/R6/R15 触发） |

## 五、核心防御主线（可执行清单）

```
信任层：链上雷达（防刷 R3） + 未验证徽章（R1） + 归属校验（R2）
安全层：URL 校验（R6） + 外链提示（R6） + 免责声明（R5）
伦理层：自愿徽章（R13） + 智能体伦理（R14） + 禁止绑架（R15）
治理层：举报通道 + 违规标记 + 社区评审（R17）
```

## 五·五、治理机制详设：举报通道与违规标记（v1.1）

### 5.1 举报通道（发现问题的入口）

> **定位（v1.1 校准）**：举报是**信息披露**入口，不是**定罪**入口。机制只负责"把分歧/疑点呈现给受益人"，永远不裁决真伪——真伪只有链上、生态、人工能裁决。因此举报产生的任何标记都是**信息性**的（"请核实"），不是惩罚性的（"有罪"）。

**流程**：贡献者卡片「举报」→ 匿名 + 分类 → 本地记录 → 计数升级

- **入口**：贡献者卡片操作区「举报」按钮
- **必选分类**：虚假贡献 / 冒领 / 钓鱼链接 / 强制付费 / 其他（空举报无效）
- **匿名**：不要求举报者身份；仅生成本地匿名编号，防同一人重复刷
- **记录格式**（本地 jsonl，隐私默认，不上传）：
  ```jsonc
  { "ts": 1780000000, "targetId": "ghost-trader", "category": "fake", "anonId": "a1b2", "note": "…" }
  ```
- **升级规则**：按**分类独立计数**——同一被举报者、同一分类、不同设备（本地匿名编号 + 按天窗口去重）累计 ≥3 条 → 该分类自动进入"有争议"；不同分类分开计数、互不叠加（实现：`computeDisputed`，红绿锁定）

### 5.2 违规标记（公示问题的状态机）

| 状态 | 触发 | 显示 | 效力（信息性，非定罪） |
|------|------|------|------|
| 🟢 正常 | 默认 / 已认证 | 绿 | — |
| ⚪ 未确认自愿性 | 无 `ethics` 声明 | 灰 | 受益者警惕（已有） |
| 🟡 有争议 | 同类举报 ≥3（自动） | 黄 | 面板展示 + 推荐位降权（**文案为"请核实"，非"有罪"**） |
| 🔴 已标记 | 人工核实成立 / `paidWall:true` / 确认钓鱼 | 红 + 原因 | 显著警示 + 生态同步（待目录配合） |

**标记防滥用（标记本身要受约束）**：
- 自动触发只到 🟡；🔴 必须人工核实（v1.2 社区评审流程）
- 申诉通道：被标记者可发起复核 → 撤销或维持（v1.2）
- 全程可追溯：标记原因 + 时间 + 来源（举报/校验/自动）存留记录
- **🟡 有限期**：同一目标同一分类 30 天无新举报 → 自动消退（防一次风波永久定罪）
- **反驳注记**：被标记者可对 🟡 写一条公开反驳（受益人同时看到举报摘要与反驳）——被标记 ≠ 只能沉默

**衔接流水线**：
```
举报（匿名+分类） → 聚合计数 → ≥3 同类 → 🟡 有争议（自动）
                                        ↓ 人工核实（社区）
                                   🔴 已标记（原因可查）→ 申诉复核 → 撤销/维持
```

### 5.3 版本切分（诚实分层）

| 部分 | 版本 | 说明 |
|------|------|------|
| 举报按钮 + 本地记录 + 自动 🟡 | v1.1 代码（红绿测试锁定计数逻辑） | 可独立交付 |
| 🟡 有限期（30 天无新举报消退）+ 反驳注记 | v1.1（文档已定，代码待补） | 防一次性风波永久定罪 |
| "疑似 fork 未声明"中性提示（归属可信度判定） | v1.1（文档已定，代码待补） | 只提示信息差距，不指控抄袭 |
| 🔴 人工核实 + 申诉 | v1.2 | 依赖社区评审流程 |
| 生态同步标记（目录/awesome） | v1.3 | 依赖目录方配合 |

### 5.4 冒名骗打赏与署名权守门（R2 深化 · 原"防抄袭骗打赏"）

> **标题校准（v1.1）**：本节原名"防抄袭骗打赏"，现改为「**冒名骗打赏 / 署名权守门**」。原因见 R2 术语校准——开源许可证让渡代码、保留署名，治理防的是**身份欺诈**，不是代码复制。

**问题（校准后）**：别有用心者复制他人的开源插件/产品，换马甲发布并挂自己的打赏地址，让受益人误以为在支持原作者 → 收割本应属于原作者的打赏。

**机制**：插件注册表条目新增 `upstream` 字段——**fork/引用其他开源项目时必须声明来源（署名义务在打赏场景的延伸）**。

```jsonc
{
  "pluginId": "dsh-tip-jar-ultimate",
  "name": "终极打赏罐",
  "contributorId": "copycat-b",
  "upstream": {                          // 🆕 fork 必填（署名义务）
    "repo": "https://github.com/luoxin10086/dsh-tip-jar",
    "author": "luoxin10086",
    "license": "MIT"
  }
}
```

**规则边界（v1.1 校准，关键）**：

| 情形 | 处理 | 理由 |
|------|------|------|
| 写了 `upstream` | 格式校验（repo URL / author 必填 / license 可选） | 署名义务的落地 |
| **没写 `upstream`（原创）** | **中性：什么都不显示** | 绝大多数插件是原创的；"没写"不是异常（开源鼓励借鉴，借鉴者未必是 fork） |
| 疑似 fork（与已收录插件同名/近名/同功能）且没写 | 中性提示："存在相似项目 X，未声明来源，请核实" | 只提示"信息差距"，不指控"抄袭" |
| **冒名（插件名/作者名与已收录原版对不上）** | 提示 + 举报（归属误导分类） | 名字对不上 = 受益人会误解归属 = 真正的欺诈 |

**核心哲学（一句话）**：
> 开源把代码让渡给了全人类，把署名留给了原作者。你可以抄代码，但不能冒名——诚实标注来源的 fork 是生态贡献；让受益人误以为在支持原作者，才是骗局。
> 规范不禁止 fork（开源本就鼓励），只要求"借用了身份必须说清楚"。

**判定原则（校准）**：机制的判定对象是**归属可信度**（"这个打赏入口的名字/作者是否清晰、可核验、不误导"），不是**代码相似度**（"像不像抄袭"）。因为代码相似度开源不管、也管不了；归属误导才是受益人受伤的环节。

**UI 提示**（v1.1 校准后）：
- `upstream` 存在但打赏地址与上游作者不同 → 面板提示"⚠️ 此插件声明上游为 X，打赏地址与上游作者不一致，请核实"（保留，L5 对账）
- 疑似 fork 且未声明 → "存在相似项目，未声明来源，请核实"（中性措辞，不带"抄袭"字样）
- **不**对"未声明 upstream 的原创插件"显示任何标记

**机制自举（v1.1）**：dsh-tip-jar 自身若参照了其他项目（如 dsh-ssh-ops 的插件范式），必须在自己的注册表条目声明 `upstream`——执法者先守法，机制才有道德合法性。

## 六、版本规划

- **v1.0（本文件）**：风险清单 + 治理框架（文档）
- **v1.1（代码）**：举报通道、违规标记、智能体 `agentPolicy` 字段、域名黑名单、支持人数去重统计
- **v1.1 校准（2026-08）**：治理定位从"防抄袭/执法"改为「**冒名防欺诈 / 信息披露**」——R2 改名、§5.1/5.2 语义校准、§5.4 重写为署名权守门、🟡 有限期 + 反驳注记、机制自举承诺
- 欢迎社区补充新风险——本文件与 ETHICS.md 一样是公共物品（CC0）

---

*配套：ETHICS.md（伦理规范）、PROJECT.md（产品规划）、manifest-spec.md（生态字段提案）。*
