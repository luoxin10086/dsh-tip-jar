# 贡献者支持系统 (Contributor Support System)

让开源插件/工具贡献者获得可持续收益的标准方案：贡献者声明支持渠道（打赏/订阅/赞助位），用户在多个入口一键触达，**纯 P2P 零经手**，隐私默认（伪匿名）。

## 1. 愿景（北极星）

> **让开源贡献者拥有多元的打赏渠道；让开源的受益者有直接支持开发者的途径；没有中间商赚差价；打赏永远是自愿的、无条件的。**

并且：**为"开源打赏"建立伦理标准**——在生态里先立好规矩，预防后来者以"打赏/订阅"之名行"强制付费"之实。

开源贡献者不应只靠"用爱发电"。本系统提供一套**标准化的贡献者收益入口**：

- **贡献者**：在注册表中声明自己的支持渠道（USDC 地址、法币平台链接、订阅页、甲方赞助位），一次声明、处处展示
- **用户**：在任何入口（设置页、会话 Tab、工具卡、生态目录）看到"谁在维护、如何支持、谁在赞助"，一键触达
- **甲方**：通过赞助位获得真实曝光（"Sponsored by X"），直接与贡献者洽谈

**设计铁律**：插件不碰钱、不代收、无 KYC、无广告追踪；资金永远 P2P 直连。

**关于"中间商"的诚实界定**：加密通道（USDC/Polygon）是**零中间商**的（链上直达，仅链上 Gas 费）；法币/订阅平台（爱发电、Ko-fi、Patreon、GitHub Sponsors 等）是**便利型渠道**，本身会抽成（约 3%-10%）——系统将其作为"用户可选的多元化入口"，但**优先推荐并突出零抽成的加密直连**，让"没有中间商赚差价"在加密通道上完全成立。

**关于"自愿性"的设计原则**：打赏**必须是自愿的、无条件的**——绝不把功能锁定在"付费墙"后，绝不让"是否打赏"影响任何用户已有的使用体验（无功能阉割、无强制弹窗、无倒计时骚扰）。订阅/会员链接只是贡献者**额外提供的**持续支持选项，用户可随时停，且永远是"主动选择"而非"被要求"。打赏的受益者在任何时候都是**自由离开的**——这正是开源精神的延伸：开源的是代码，自愿的是支持。

## 2. 已交付（MVP，本会话）

| 交付物 | 位置 | 状态 |
|--------|------|------|
| 注册表 | `sponsors.json`（工作区根） | ✅ 示例伪匿名数据 |
| 校验逻辑 | `dsh-sponsors/validate.js` | ✅ 6 项测试全绿 |
| 测试 | `dsh-sponsors/registry.test.js` | ✅ 红绿闭环 |
| DSH 插件 | `sps-2/pkg-5`（动态插件） | ✅ 运行中（run-6 激活，三入口已验证） |
| 三入口 | 会话"支持"Tab / 设置页"支持贡献者" / `pm_trading_status` 工具卡致谢 | 代码就绪 |

## 3. 规范 ↔ 实现对照表

> 规范版本（v1.x）与插件版本（0.1.x）**刻意不一致**：规范先行（立标准），代码跟进（文档先行原则）。此表是两者唯一的对齐锚点。

| 规范条款 | 规范版本 | 插件版本 | 状态 |
|---------|---------|---------|------|
| 自愿打赏 / 无付费墙 / 透明 / 隐私 / 零中间商 | ETHICS v1.0 | 0.1.4 | ✅ 已实现 |
| 伦理徽章（🟢自愿 / ⚪未确认 / 🔴付费墙） | ETHICS v1.0 | 0.1.5 | ✅ 已实现 |
| 链上到账雷达（Polygon USDC + 致谢墙） | 风险治理 v1.0（R3 防刷） | 0.1.5 | ✅ 已实现 |
| 注册表校验（地址/URL/引用/ethics） | manifest v0.1 | 0.1.4-0.1.5 | ✅ 已实现 |
| upstream 来源声明（防抄袭 L2） | 风险治理 v1.1 | 0.1.6 | 📋 规划 |
| 举报通道 + 自动🟡争议标记 | 风险治理 v1.1 | 0.1.6 | 📋 规划 |
| 支持人数去重统计 | 风险治理 v1.1 | 0.1.6 | 📋 规划 |
| 🔴已标记人工核实 + 申诉 | 风险治理 v1.2 | 0.2.x | 🕓 规划 |
| 智能体打赏伦理 agentPolicy | ETHICS v1.1 | 远期 | 📋 规划 |
| 多链雷达 / 二维码图片通道（微信/支付宝/数币） | — | 0.1.6+ | 💡 候选 |

**约定**：文档中"✅ 已实现"的标记必须能对应到上表某一行；新功能先写进规范/本表，再动代码。

## 4. 架构

```
sponsors.json（注册表：贡献者 + 插件 + 赞助位）
   │  Host 读取 + 校验（与 validate.js 同规则）
   ▼
harness.handle('sponsors-list')  ←── host.call（Client→Host JSON RPC）
   ▼
Client 渲染三入口
   ① conversation.view "支持" Tab（赞助中心）
   ② settings.section "支持贡献者" 页
   ③ tool.call.toolview 工具卡致谢（Sponsored by / 支持作者）
```

## 4. 注册表 Schema（v1）

```jsonc
{
  "schemaVersion": 1,
  "privacyNote": "…",
  "contributors": [{
    "id": "唯一ID",            // 必填，去重
    "alias": "化名",            // 必填，隐私默认：不强制真实身份
    "verified": false,          // 必填；false → UI 显示"未验证"
    "bio": "一句话介绍",         // 可选
    "tips": {
      "usdc": "0x…(40hex)",     // 可选，格式校验 /^0x[0-9a-fA-F]{40}$/
      "chains": ["polygon"],
      "fiat": [{ "label": "爱发电", "url": "https://…" }]  // 可选
    },
    "subscriptions": [{ "label": "Patreon", "url": "https://…" }]  // 可选
  }],
  "plugins": [{
    "pluginId": "插件ID",       // 必填，去重
    "name": "插件名",
    "contributorId": "引用 contributors.id",  // 引用完整性校验
    "sponsors": [{ "name": "甲方", "message": "标语", "url": "https://…" }]  // 赞助位
  }]
}
```

校验规则（`validate.js`，已测试）：结构必填、USDC 地址格式、URL 格式、引用完整性、重复 id、schemaVersion。

## 5. 生态规范草案：插件 manifest `sponsor` 字段

正式项目阶段向生态提议：插件元数据增加 `sponsor` 声明，与注册表互操作。

```yaml
# 插件 manifest 提案
sponsor:
  contributorId: ghost-trader      # 关联注册表
  tips: { usdc: true }             # 支持 USDC
  fiat: ["afdian", "github-sponsors"]
```

生态工具（目录页、awesome 列表）按规范读取，自动聚合展示。

## 6. 隐私与信任模型

| 原则 | 实现 |
|------|------|
| 伪匿名默认 | 贡献者只须提供 alias，不强制真实身份/GitHub 绑定 |
| 未验证标记 | `verified:false` → UI 黄色"未验证"徽章；冒用风险自担，UI 含免责文案 |
| 认证（二期） | GitHub 账号签名声明，或生态收录时统一登记 |
| 数据主权 | 注册表是显式声明，无自动发现、无爬取 |

## 7. 安全与合规

- **纯 P2P**：用户直接向贡献者地址/平台转账，系统零经手 → 无支付牌照需求、无 KYC
- **地址校验**：非法地址在展示前被拒绝（测试锁定）
- **QR 码**：外部服务（api.qrserver.com），失败降级为"选择复制"
- **降级**：注册表缺失/损坏 → 空态 + 引导文案，不崩溃
- **资金安全提示**：UI 提示用户核对链与地址（Polygon USDC）

## 8. Roadmap

| Phase | 内容 | 状态 |
|-------|------|------|
| **P1 MVP** | 注册表 + 校验 + 三入口动态插件 | ✅ 完成并运行（run-6） |
| **P2 正式项目化** | 独立 repo（本目录）；manifest `sponsor` 字段规范；生态目录页；README 英文版；**动态 Client 模块跨页面加载持久化**（见下方"已知限制"） | ⏳ 进行中 |
| **P3 生态认证** | GitHub 绑定验证；awesome-deepseek-harness 收录；可选 OpenCollective 托管 | 规划 |
| **P4 赞助位市场** | 甲方自助下单、曝光统计（**注意**：统计需用户同意，默认关闭） | 规划 |

## 9. 生态集成

- **awesome-deepseek-harness**：以插件/规范形式收录，贡献者凭此获得曝光
- **GitHub Sponsors / 爱发电 / Ko-fi / Patreon**：作为 fiat/subscriptions 链接目标，复用成熟平台
- **USDC (Polygon)**：主打加密渠道，与 Polymarket 生态同链，用户熟悉

## 10. 风险与失败模式

| 风险 | 应对 |
|------|------|
| 冒领他人插件打赏 | 未验证标记 + 免责声明；二期认证 |
| 支付平台链接失效 | 链接由贡献者自维护，UI 提示"如失效请联系作者" |
| QR 服务不可达 | 图片占位，复制地址仍可用 |
| 注册表被误改坏 | 校验报错 + 空态，不崩溃 |
| 广告赞助位变成弹窗骚扰 | 规范约定：仅静态展示，禁弹窗/追踪（非目标） |
| 页面刷新后动态 Client UI 消失 | 现状：动态插件的 Client 模块在**激活时**注入已连接页面，新页面/刷新后不自动恢复（实测 trd-1 与 sps-2 均复现）。临时恢复：重新 `cordis_run` 该插件（推送到当前连接页面）。**P2 待办**：向 DSH 提 feature/PR——动态 Client 模块应跨页面加载持久化（如按 session 在页面加载时重新拉取模块清单） |

---

*本文件为正式项目规划；MVP 代码与测试见 `dsh-sponsors/`。*
